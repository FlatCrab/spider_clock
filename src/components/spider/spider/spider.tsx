import { Spiderbody } from "../spiderbody/spiderbody";
import { SpiderLeg } from "../spiderleg/spiderleg";
interface SpiderProps {
    colour: string,
    rotation: number,
    mirrorX: boolean,
    mirrorY: boolean,
}

const Spider = ({ colour, rotation, mirrorX, mirrorY }: SpiderProps) => {
    return (
        <div>

        </div>
    );
};